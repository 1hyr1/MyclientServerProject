import socket
import sys

def send_request(server_host, server_port, request_file):
  client_socket = socket.socket(socket.AF_INET, server_port))

  with open(request_file,'r') as file:
       for line in file:
           line = line.strip()
           if not line:
              continue

           request = f"{len(line):03d} {line}"

  client_socket.sendall(request.encode('uft-8').strip()
           print(f"{line}: {response}")

        client_socket.close()

  if__name__=="__main__"
      if len(sys.argv) !+ 4:
         print("Usage: Python client.py
  <server_host> <server_port> <reuest_file>")
         sys.exit(1)

